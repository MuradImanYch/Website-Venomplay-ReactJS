import React, { useEffect } from 'react';

const Game = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return (
        <div>
            wefwefwe
        </div>
    );
};

export default Game;